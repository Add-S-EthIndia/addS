
const WalletDashboard = () => {
  return (
    <div>
      after connecting check if they already affliate then put a check that you
      are already affliate
    </div>
  );
};

export default WalletDashboard;
